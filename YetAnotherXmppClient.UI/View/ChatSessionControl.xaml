<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Border BorderThickness="1" BorderBrush="Black">
    <Grid RowDefinitions="*,Auto">
      <ListBox Items="{Binding Messages}" Name="msgListBox"  MinHeight="100" Margin="3" />
      <!--<ScrollViewer>
        <ItemsControl  ItemsSource="{Binding Messages}" x:Name="tStack" Grid.Column="0">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal" IsItemsHost="True"/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding}"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>-->
      <Grid ColumnDefinitions="*,Auto" Grid.Row="1">
        <TextBox Text="{Binding TextToSend}"  HorizontalAlignment="Stretch" Grid.Column="0" />
        <Button Content="Send" Name="sendButton" IsDefault="True" Grid.Column="1" />
      </Grid>
    </Grid>
  </Border>
</UserControl>